# Exact dependency versions used to generate the numbers reported in paper/main.tex
# and reports/*.csv (regenerated 2026-07-08). Install with:
#   pip install -r requirements-lock.txt
#   pip install -e .
# Fold-level results from StratifiedKFold/train_test_split are not guaranteed
# to be bit-identical across scikit-learn versions even with a fixed random_state,
# because the internal shuffling implementation can change between releases.
# Use this lockfile, not the loose bounds in requirements.txt, when trying to
# reproduce the exact tables in the paper.
python==3.12.10
numpy==2.5.1
pandas==3.0.3
scikit-learn==1.9.0
scipy==1.18.0
sentence-transformers==5.6.0
